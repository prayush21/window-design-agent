.DS_Store
node_modules/
.env
.env.*
!.env.example
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# eval harness outputs
evals/runs/
evals/cache/

# Incoming vendor catalog drop — 199MB of images. Track with git-lfs or an object
# store rather than committing blobs that can never be removed from history.
Product Catalog/
